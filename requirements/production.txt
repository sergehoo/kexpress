# Kaydan Express — dépendances de production
-r base.txt

# Service de fichiers statiques (admin, DRF, OpenAPI) intégré à l'app ASGI
whitenoise>=6.7

# Monitoring d'erreurs / performance (activé si SENTRY_DSN est défini)
sentry-sdk>=2.0
